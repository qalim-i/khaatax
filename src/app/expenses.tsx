import { PhasePlaceholder } from '@/components/ui/phase-placeholder';

export default function ExpensesScreen() {
  return (
    <PhasePlaceholder
      title="Expenses"
      phaseLabel="Coming in Phase 2"
      description="Expense logging, category breakdown, and the monthly trend chart land in the next phase."
    />
  );
}
